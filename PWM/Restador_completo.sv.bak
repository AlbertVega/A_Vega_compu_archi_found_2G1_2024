module Restador_completo (
			input logic A, B, Cin,
			output logic Cout, Resta
);

always_comb begin
	Cout = ((not A) and B) or (Cin and (not A)) or (Cin and B);
	Resta = (A xor B) xor Cin;
	
end

endmodule